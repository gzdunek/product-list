import React from 'react';

import './Header.scss';

const Header = () => (
    <div className="header">
        <h2 className="header__logo"><i className="far fa-square"/></h2>
        <h2 className="header__title">Shop name</h2>
    </div>
);

export default Header;